#include <iostream>

using namespace std;

string solve(string u) {
  vector <int> f(0, 10);
  for(int i = 0; i < u.size(); ++i) f[u[i] - '0']++;
}

int main() {
  string u;
  while(cin >> u) {

  }
}
